import { Typography } from "components/elements/typography";
import s from "./listCategories.module.scss";

export const ListCategories = () => {
    return (
        <section className={s.container}>
            <Typography>listCategories</Typography>
        </section>
    );
};
