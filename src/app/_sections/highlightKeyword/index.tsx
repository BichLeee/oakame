import { Typography } from "components/elements/typography";
import s from "./highlightKeyword.module.scss";

export const HighlightKeyword = () => {
    return (
        <section className={s.container}>
            <Typography>highlightKeyword</Typography>
        </section>
    );
};
